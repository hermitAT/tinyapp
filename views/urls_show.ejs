<!DOCTYPE html>
  <html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css">
    <title>tinyApp - shortURL</title>
  </head>
  <body>
    <%- include('partials/_header') %>
    <div class="card text-center">
        <div class="card-header"></div>
        <div class="card-body">
          <h5 class="card-title">tinyURL for: <%= longURL %></h5>
          <p class="card-text">tinyURL: <a href="/u/<%= shortURL %>"><%= shortURL %></a></p>
        </div>
        <div class="card-footer text-muted">
        <h5 class="card-title">Update your tinyURL!</h5>
        <form class="form-inline-center" method="POST" action="/urls/<%= shortURL %>?_method=PUT">
          <div class="form-group mb-2">
            <label for="longURL">New longURL:</label>
            <input class="form-control-center" type="text" name="longURL" placeholder="http://" style="width: 400px; margin: 1em">
            <button type="submit" class="btn btn-primary">Submit</button>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>