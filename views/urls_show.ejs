<!DOCTYPE html>
  <html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css">
    <title>TinyApp - shortURL</title>
  </head>
  <body>
    <%- include('partials/_header') %>
    <div class="card text-center">
        <div class="card-header"></div>
        <div class="card-body">
          <h5 class="card-title">TinyURL for: <%= longURL %></h5>
          <p class="card-text">Short URL: <a href="/u/<%= shortURL %>"><%= shortURL %></a></p>
        </div>
        <div class="card-footer text-muted">
        <h5 class="card-title">EDIT</h5>
        <form class="form-inline-center" action="/urls/<%= shortURL %>" method="POST">
          <div class="form-group mb-2">
            <label for="longURL">New URL:</label>
            <input class="form-control-center" type="text" name="longURL" placeholder="http://" style="width: 400px; margin: 1em">
            <button type="submit" class="btn btn-primary">Submit</button>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>